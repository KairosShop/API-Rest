runtime: nodejs10
service: staging

handlers:
- url: /.*
  secure: always
  redirect_http_response_code: 301
  script: auto

env_variables:
    PORT: 8080
    CORS: "*"
    SENTRY_DNS: "<% SENTRY_DNS %>"
    SENTRY_ID: <% SENTRY_ID %>